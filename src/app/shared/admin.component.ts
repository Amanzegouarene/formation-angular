import { Component } from '@angular/core';

@Component({
    // Pas de slecteurs, pas besoin de les utiliser ailleurs (will be dynamic invoked)
    templateUrl: './admin.component.html'
})
export class AdminComponent {
    constructor() { }

}